// const question = require('./myModul/quest');

// // fungsi utama
// const main = async () => {
//     const nama = await question.question('Masukan Nama Anda');
//     const hp = await question.question('Masukan Nomor Hp Anda');
//     const email = await question.question('Masukan Email Anda');

//     question.saveData(nama,email,hp);

// };

// main();